# Fixture: contains a FAKE key with an invalid value (never a real credential).
# Used only for codespot selftest. Note: the classic AWS docs key
# "AKIAIOSFODNN7EXAMPLE" is allowlisted by gitleaks, so we use a distinct fake.
aws_access_key_id = "AKIAABCDEFGHIJKLMNOP"
